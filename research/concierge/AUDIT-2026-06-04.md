# Audit — Concierge research + spec artifacts (2026-06-04)

**Auditor:** Claude (per Abu's call-out to audit before continuing remaining story batch)
**Scope:** `research/concierge/` + `docs/` — verify domain knowledge facts before sahil-spec-writer's remaining 84 stories ride on them
**Verdict:** **Domain knowledge solid; 4 contamination patches applied + all 7 high-stakes on-chain claims re-verified today.**

---

## Method

1. Pattern-grep for Patron-era contamination ("Hold/YieldBNPL", "spend without selling", "BNPL", "PatronVault", stale rate claims)
2. Re-run `cast call` against Mantle Mainnet (`https://rpc.mantle.xyz`) for every load-bearing address claim
3. Verify SDK package existence via `gh api` (Context7 fetch failed twice today — likely transient)
4. Cross-check addresses referenced in stories vs `@mpilot/shared` addresses module vs source provider docs
5. Sanity-check test count claims per story

---

## Findings

### 1. Patron-era contamination — **4 patches applied** ✅

| File | Line | Before | Action |
|---|---|---|---|
| `research/concierge/03-providers/aave-v3-mantle.md` | 4 | "single most load-bearing action provider for the locked Hold/YieldBNPL wedge: every 'spend without selling' tick routes through an Aave borrow" | Rewrote as autonomous DeFi agent framing (yield + risk management + tick-loop reads) |
| `research/concierge/03-providers/ethena-susde.md` | 4 | "primary yield-bearing collateral for the locked Hold/YieldBNPL wedge — deposit user's stablecoin → swap to sUSDe → supply to Aave E-Mode 1 → borrow spendable USDC..." | Rewrote as standalone yield position + borrow-side collateral, with explicit spread-floor refusal mechanic |
| `research/concierge/06-realclaw-skill-pkg.md` | 167 | **CRITICAL** — SKILL.md description called Concierge a "Buy-Now-Pay-Later agent" that "pays merchants from yield" — this string ships in the published skill manifest | Rewrote as "autonomous AI agent for Mantle DeFi" with goal/tick framing |
| `research/concierge/06-realclaw-skill-pkg.md` | 338, 343 | Open questions + trigger phrases referenced "mantle-yield-bnpl" + "BNPL on Mantle" + "spend without selling" + "yield-funded payments" | Rewrote name option as `mantle-agent`; replaced trigger phrases with autonomous-DeFi-agent-aligned set |
| `docs/architecture.md` | 258 | ADR-008 said "USDC peg hardcoded at $1 in **PatronVault** as defense-in-depth" — wrong contract name | Rewrote as `@mpilot/aave-v3-mantle` price-read helper |

**Why these matter:**

- The SKILL.md description in `06-realclaw-skill-pkg.md` is **judge-visible** — it ships in the npm-published skill manifest and shows up in `npx skills add` discoverability. The fix was the highest-priority finding.
- The provider docs are read by `sahil-spec-writer` for the remaining ~84 story files. Contaminated source → contaminated stories → contaminated build briefs.
- The ADR-008 reference to "PatronVault" would have leaked into any story citing the ADR.

### 2. Intentional historical references kept (NOT contamination)

These "Patron" mentions are correct context — the spec writer + future agents need to know what the predecessor wedge was and why we moved away:

| File | Line | Purpose |
|---|---|---|
| `docs/architecture.md` | 43, 272 | "Patron-pattern mocks reused" + ADR-012 "reuse Patron pattern" — explains the Sepolia mock-deploy strategy genesis |
| `research/concierge/01-wedge-locked.md` | 44 | "vs original Patron (BNPL) — Concierge isn't yield-spread-dependent" — explains why this wedge beats the predecessor |
| `research/concierge/09-tracks-and-judges.md` | 79 | "Abu has fatigue on RWA-shaped wedges (lost Patron there)" — explains why AI×RWA track is skipped |
| `research/concierge/02-architecture.md` | 116 | ADR-007 lineage — "lifted from Patron ADR-007 which was re-revised after the audit-of-audit caught Biome's nursery rule" — preserves the audit lesson |
| `research/concierge/06-realclaw-skill-pkg.md` | 343 | Trigger phrases now say "(no BNPL framing — that was the paused Patron wedge)" — clarifies the rewrite |

All kept intact.

### 3. Chainlink mentions outside ADR-008 explanation — all explainable

| File | Line | Context | Verdict |
|---|---|---|---|
| `docs/epics.md` | 44 | "(OZ v5, Aave V3 Origin, NO Chainlink)" — story-03 description | Intentional negative reference per ADR-008 |
| `docs/stories/story-03-foundry-init-and-remappings.md` | 1, 14, etc. | Title + body explains why no Chainlink install | Intentional |
| `research/concierge/03-providers/ethena-susde.md` | 152 | "Mantle oracle (Redstone/Chainlink) reports the new price" | Describes the *underlying* feed inside the Aave Capped composite — does NOT contradict ADR-008 (we still read via `IAaveOracle`, not the raw Chainlink/Redstone feed) |
| `research/concierge/03-providers/mantle-dex.md` | 118, 208 | WOOFi prices off Chainlink + internal feed | Describes WOOFi's own mechanism, not Concierge's price source |
| `research/concierge/03-providers/meth-staking.md` | 82, 132, 170 | mETH cross-chain exchange-rate reads via Redstone/Chainlink on Mantle | Describes mETH protocol's price-relay mechanism |
| `research/concierge/03-providers/aave-v3-mantle.md` | 143 | Error-handling note: "Mantle oracle uses Chainlink + Redstone" | Mechanism explanation for `PRICE_ORACLE_SENTINEL_CHECK_FAILED` |

All consistent with ADR-008 (we never read raw Chainlink/Redstone directly; we read via Aave Oracle's Capped composites which internally use those feeds).

### 4. On-chain claims re-verified today (2026-06-04 1:26pm GMT+1)

All re-verified via `cast call --rpc-url https://rpc.mantle.xyz`:

| Claim | Verification | Result |
|---|---|---|
| Aave V3 Pool `0x458F293454fE0d67EC0655f3672301301DD51422` lives at the documented address | `ADDRESSES_PROVIDER()` returns `0xba50Cd2A20f6DA35D788639E581bca8d0B5d4D5f` (matches bgd-labs canonical) | ✅ |
| ERC-8004 Identity Registry `0x8004A169FB4a3325136EB29fA0ceB6D2e539a432` | `getVersion()` returns `"2.0.0"` | ✅ |
| ERC-8004 Reputation Registry `0x8004BAa17C55a88189AE136b182e5fdA19dE9b63` | `getIdentityRegistry()` returns `0x8004A169...` (correct cross-link) | ✅ |
| mETH on Mantle `0xcDA86A272531e8640cD7F1a92c01839911B90bb0` | `name()` returns `"mETH"`, totalSupply `~28,747 mETH` (drift from 28,827 yesterday — normal staking activity) | ✅ |
| USDY on Mantle `0x5bE26527e817998A7206475496fDE1E68957c5A6` | `name()` returns `"Ondo U.S. Dollar Yield"`, decimals 18 | ✅ |
| Li.Fi Diamond `0x1231DEB6f5749EF6cE6943a275A1D3E7486F4EaE` | Bytecode present | ✅ |

All addresses + claims hold. **No drift since the 2026-06-03 verification pass.**

### 5. SDK package existence verified via gh

Context7 fetches failed twice today with "fetch failed" (transient). Used `gh api` instead:

| Library | Verification | Result |
|---|---|---|
| `@anthropic-ai/claude-agent-sdk` | `gh api repos/anthropics/claude-agent-sdk-typescript` — 1492 stars, pushed 2026-06-03 (yesterday — actively maintained), not archived | ✅ Real package, fresh activity |
| `permissionless.js` (Pimlico SDK) | `gh api repos/pimlicolabs/permissionless.js` — "TypeScript utilities built on viem for ERC-4337: Account Abstraction", 251 stars, pushed 2026-05-27 | ✅ Real package, active |

Pimlico Mantle support was previously verified by the runtime research agent from `docs.pimlico.io/infra/platform/supported-chains` (recorded in `_RUNTIME_SUMMARY.md` § verified facts). No reason to revisit unless Pimlico drops Mantle (unlikely).

### 6. Cross-file address consistency — all aligned

Addresses referenced in `docs/stories/story-20-shared-package-bootstrap.md` BDD assertions match exactly:

- `ADDRESSES.mantleMainnet.erc8004.identityRegistry` → `0x8004A169...` ✅
- `ADDRESSES.mantleMainnet.aave.pool` → `0x458F293454...` ✅
- `ADDRESSES.mantleMainnet.lifi.diamond` → `0x1231DEB6...` ✅

All match the verified canonical addresses from provider docs.

### 7. Magic numbers in story BDD blocks — acceptable

Chain id literals `5000` and `5003` appear in story-20 (notes) + story-24 (config-loader test cases). These are:
- In **BDD test assertions** (correct — tests verify literal values)
- In **explanatory notes** for the coding agent (correct — explains the chain ids the runtime uses)

The runtime code itself MUST use `@mpilot/shared` constants (`ADDRESSES.mantleMainnet`, chain-id enum). The architecture.md banned-patterns list already enforces this. No patches needed in stories; this is a coding-agent runtime rule, not a story-spec rule.

### 8. Test count claims — reasonable per story scope

| Story | Test count claim | Verdict |
|---|---|---|
| story-20 (shared-package-bootstrap) | ≥ 12 cases asserting addresses match `/^0x[a-fA-F0-9]{40}$/` | Achievable — ~15 addresses across both networks × shape check |
| story-21 (shared-abi-imports) | ≥ 6 ABI sanity cases | Achievable — 4-5 key functions per ABI × 3 ABIs |
| story-22 (sdk-skeleton) | ≥ 15 cases (3 construction + 3 registration + 3 lookup + 3 lifecycle + 3 invalid-config) | Achievable |
| story-23 (sdk-error-types) | ≥ 12 cases for instance checks + serialization | Achievable — 12+ error classes |
| story-24 (sdk-config-loader) | ≥ 10 cases (happy + missing + invalid URL + coercion + edge cases) | Achievable |

All counts realistic; coding agent should hit them with first-pass behavioral tests.

---

## Process gaps + retry notes

- **Context7 was down** both times during today's audit pass — couldn't query `/vercel/ai`, ZeroDev, Pimlico libraries fresh. Verified via `gh api` instead. **Action:** if writing more stories that need Context7 (e.g., generative-UI patterns), retry Context7 first; if still down, use `gh api` + WebFetch as the fallback chain.
- **RPC failed first attempt with DNS error**, succeeded on retry. **Action:** if RPC fails in coding-agent stories, the BDD spec already requires retry semantics (`oracle stale → wait 1 block → retry` per aave-v3-mantle.md). No spec change needed.

---

## Verdict

**Domain knowledge folder is solid for sahil-spec-writer's remaining 84 stories.** The 4 contamination patches close the load-bearing risks. All other Patron references are intentional historical context. All on-chain addresses re-verified today. All SDK package names confirmed real.

**Next step:** resume story writing (E1 contracts → E3 providers → E4 smart account → E5 runtime → E6 attestation → E7 web app → E8 MCP → E9 skill → E10 docs → E11 deploy → E12 submission).

**One housekeeping note for future audits:**
- Add a `_AUDIT_INDEX.md` at `research/concierge/_AUDIT_INDEX.md` listing every audit pass + verdict + patches. This is the first; subsequent audits append.
