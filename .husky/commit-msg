#!/usr/bin/env sh
# Enforce Conventional Commits via commitlint. `--ignores 'merge,revert'`
# lets git's autogenerated merge/revert messages through (those don't
# conform to Conventional Commits and the orchestrator's squash-merge
# flow rarely produces them anyway).
pnpm exec commitlint --edit "$1"
