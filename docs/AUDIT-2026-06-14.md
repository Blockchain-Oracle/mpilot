# Plan-vs-Shipped Audit — 2026-06-14

**Trigger:** Abu, 2026-06-13 late: *"audit what we have so far... go check the research folder... read the PRD... go look at the plan so you see what we plan on having and audit it against what we currently have."*

**Method:** read `research/concierge/` (CONTEXT, 01-wedge-locked, 02-architecture, 04-runtime, 10-constraints, _RUNTIME_SUMMARY, AUDIT-2026-06-09); cross-checked against `docs/PRD.md`, `docs/sprint-status.yaml` (122 stories), filesystem state of `packages/`, `apps/`, `contracts/`.

---

## 1. Headline numbers

| Metric | Value |
|---|---|
| Total stories | **122** |
| COMPLETE | **69 (57%)** |
| PENDING | **53 (43%)** |
| IN_PROGRESS | 0 |
| Package directories present | 17 (14 with `package.json` + 3 stubs) |
| ADRs locked | 19 |
| Surfaces promised | 4 (web · SDK · MCP · Skill) |
| Surfaces actually buildable end-to-end today | **1** (Skill, partially) |

---

## 2. PRD-promised vs. shipped — 15-package architecture

PRD + ADR-014/015/018 lock 15 packages: foundation (`shared`, `agent`, `tools`) + 7 providers + 5 adapters (`vercel-ai`, `openai`, `langchain`, `agentkit`, `mcp`) + 2 React (`react`, `react-ui`) + `ui` (tokens) + `skill` + `sdk`.

| Plan | Shipped | Status |
|---|---|---|
| `@concierge/shared` | `packages/shared/` (8 src files) | ✅ |
| `@concierge/agent` (E5 runtime) | `packages/runtime/` (named differently — renamed?) | ⚠️ NAMING DRIFT |
| `@concierge/tools` | `packages/tools/` (9 src files) | ✅ |
| `@concierge/aave-v3-mantle` (standalone) | `packages/providers/aave-v3-mantle/` (nested) | ⚠️ SHAPE DRIFT |
| `@concierge/mantle-dex` | `packages/providers/mantle-dex/` | ⚠️ SHAPE DRIFT |
| `@concierge/ethena-susde` | `packages/providers/ethena-susde/` | ⚠️ SHAPE DRIFT |
| `@concierge/ondo-usdy` | `packages/providers/ondo-usdy/` | ⚠️ SHAPE DRIFT |
| `@concierge/meth-staking` | `packages/providers/meth-staking/` | ⚠️ SHAPE DRIFT |
| `@concierge/lifi-bridge` | `packages/providers/lifi-bridge/` | ⚠️ SHAPE DRIFT |
| `@concierge/erc8004` | `packages/providers/erc8004/` | ⚠️ SHAPE DRIFT |
| `@concierge/vercel-ai` | `packages/vercel-ai/` | ✅ |
| `@concierge/openai` | `packages/openai/` | ✅ |
| `@concierge/langchain` | `packages/langchain/` | ✅ |
| `@concierge/agentkit` | `packages/agentkit/` | ✅ |
| `@concierge/mcp` | `packages/mcp/` (6 src) | ✅ |
| `@concierge/react` (headless) | **MISSING** | ❌ (story-310 PENDING) |
| `@concierge/react-ui` (styled) | **MISSING** | ❌ (story-311 PENDING) |
| `@concierge/ui` (brand tokens) | `packages/ui/` — **empty stub** (no package.json) | ❌ |
| `@concierge/skill` | `packages/skill/` — **empty stub** + `packages/skill-mantle-agent/` real | ⚠️ TWO PKGS, ONE EMPTY |
| `@concierge/sdk` (meta) | `packages/sdk/` (6 src) | ✅ |

**Extra packages not in plan:** `attestation/`, `db/`, `llm/`, `smart-account/` — these are useful infra splits that match Epic E2/E4/E5 stories but aren't in the headline 15-package list. Recommend updating PRD §packages.

**Shape drift call:** Providers being NESTED under `packages/providers/<name>/` instead of being top-level workspace packages means `@concierge/aave-v3-mantle` may not be publishable as advertised. Verify each provider has its own `package.json` + scoped name. (Filesystem shows the dirs exist but I didn't open each — pre-publish gate.)

---

## 3. Surfaces — buildable end-to-end?

PRD promises 4 surfaces. Reality:

| Surface | Plan target | Shipped state | Gap |
|---|---|---|---|
| **Web app** (`concierge.xyz/app`) | Next.js 15 + landing + dashboard + tick stream + reputation | `apps/web/src/` contains a single `index.ts` stub | ❌ entire frontend pending (stories 100–115 all PENDING + 310–314 React packages) |
| **MCP server** (stdio-first per ADR-011) | `packages/mcp/` core + `apps/mcp/` Cloudflare Worker variant | `packages/mcp/` shipped + stdio bin (story-130 COMPLETE); `apps/mcp/index.ts` is stub | ⚠️ core ships, hosted variant stub (stories 131–138 PENDING) |
| **Skill** (`npx skills add ...`) | SKILL.md + scripts + npm publish | `packages/skill-mantle-agent/` real (SKILL.md + scripts + schemas + assets) | ⚠️ publish + distribution PRs PENDING (152–154) |
| **SDK** (`@concierge/sdk` + adapters) | meta package + 4 adapters | `packages/sdk/` + 4 adapter packages all scaffolded with `index.ts` | ✅ closest to done; verify adapters past stub |

Only ONE surface (Skill) is close to user-facing today, and even it can't run without provider implementations being publishable.

---

## 4. Contracts — Epic E1

Plan: `ConciergeRegistry` UUPS proxy + session-key validator + Sepolia mocks (`MockAavePool` pattern).

Shipped: `contracts/src/ConciergeRegistry.sol` + `ConciergeRegistryProxy.sol` + `Placeholder.sol` + `errors/` + `interfaces/` + `mocks/`. Story-10/11/12/13 (registry + tests + fuzz + invariant) COMPLETE per sprint-status. Story-190 deploy runbook COMPLETE; story-192 Sepolia mock deploy + story-193 Mainnet deploy execution PENDING.

**Gap:** the actual on-chain Mainnet broadcast hasn't happened. Submission requirement (Mantle prize doc): "Smart contract deployed on Mantle Mainnet (chain id 5000) + verified on MantleScan." Story-193 is the blocking story for prize eligibility.

---

## 5. 7 Providers — Epic E3

All 7 directories exist under `packages/providers/`. Provider sprint-status: per memory observation 19942 ("25 granular actions across 7 providers"), implementations are present in the runtime. But the audit hasn't sampled per-provider story status; recommend a follow-up sweep.

Load-bearing gotcha check (CLAUDE.md):
- ✅ Aave E-Mode 1 trap documented in 03-providers/aave-v3-mantle.md
- ✅ ERC-8004 canonical addresses documented + verified 2026-06-04
- ⚠️ Story-42 EIP-712 vs research/erc8004.md raw-keccak contradiction OPEN (flagged in `writeAttestation.ts` JSDoc) — BLOCKS story-84 v1.1 hash verification

---

## 6. The 53 PENDING stories — what's missing for submission

Grouped by ship-blocking impact for the Mantle Turing Test 2026 submission:

### Blocker class A — required for prize eligibility (per `10-constraints.md`)
- **story-193** — Mainnet deploy execution (contract on chain 5000 + MantleScan verify)
- **story-194** — MCP server deployment (`mcp.concierge.xyz` publicly accessible)
- **story-195** — Post-deploy smoke tests
- **story-100..115** — Entire Next.js app (16 stories). Without this there is NO `concierge.xyz/app` → fails Best UI/UX + Grand Champion + 20-Project Deployment Award eligibility.
- **story-153** — Skill npm publish (Track 6 Agentic Economy requirement: `npx skills add` must work)
- **story-200..205** — README finalize / architecture diagram / demo video (≥ 2 min per 20-Project rule) / X thread / DoraHacks submission / live demo rehearsal

### Blocker class B — quality-bar work needed for credible demo
- **story-56** — Smart-account tests
- **story-61** — Vercel AI SDK chat API
- **story-108..111** — TickCard / GoalSetScreen / TickHistoryList / TickDetailPage (the demo primitives per UX spec)
- **story-310..314** — React headless + styled + dogfood + assistant-ui + copilotkit adapters
- **story-137, 138** — MCP Apps `ui://` + Elicitation (ADR-017 three-rail gen UI)
- **story-152, 154** — Skill JSON output contract + distribution PRs

### Blocker class C — supporting
- **story-170..177** — Docs site (8 stories)
- **story-191** — Sepolia faucet page
- **story-192** — Sepolia playground deploy
- **story-330** — Scaffolder

---

## 7. Specs vs. code drift to fix

| # | Drift | Fix |
|---|---|---|
| 1 | PRD §packages says 15 names; tree has 17 dirs (with infra splits). | Update PRD to list `attestation`, `db`, `llm`, `smart-account` as documented internal packages OR fold their contents back into `shared`/`agent`. |
| 2 | `packages/runtime/` ↔ `@concierge/agent` (planned name in ADR-014). | Pick one and rename consistently. SDK-DX-STUDY examples use `@concierge/agent`. Recommend rename `runtime` → `agent` before publish. |
| 3 | `packages/skill/` AND `packages/skill-mantle-agent/` both exist. | Delete the empty stub, OR clarify if one is the framework and one is the published skill. |
| 4 | `packages/ui/` is empty (no `package.json`). | Either ship brand tokens here (per ADR-018) before designer hands off, or delete and document deferral. |
| 5 | Providers nested vs top-level publishability. | Verify each provider has scoped `package.json`; if not, add per-package configs before story-153/publish step. |
| 6 | Story-42 EIP-712 vs raw-keccak attestation hash. | Per CLAUDE.md non-negotiable (d), research/erc8004.md wins → fix `writeAttestation.ts` before story-84 v1.1 verification work. |

---

## 8. What we are NOT forgetting (sanity-check against `01-wedge-locked.md`)

Pulled directly from the wedge brief — all still in-spec:
- ✅ Autonomous agent on Mantle with plain-English goal → plan/sim/propose/execute/record loop — runtime + phases shipped (story-19942: TickLoopPhase + 6-phase phases dir).
- ✅ ERC-8004 attestation per successful tick — `packages/attestation/` shipped; provider 03-providers/erc8004.md verified.
- ✅ Four surfaces — promised in PRD §1; **execution incomplete** for 3 of 4.
- ✅ Sepolia playground for zero-capital judge access — chain-id routing in HelperConfig per ADR-008/012; story-192 deploy PENDING.
- ✅ Track 6 via RealClaw skill — `skill-mantle-agent` real; publish PENDING.
- ❌ "Watch it bridge, swap, lend, borrow and rebalance in real time" — requires web app (story 108 TickCard); **PENDING**.

---

## 9. Recommended next-story selection (per CLAUDE.md §1)

The current PENDING set whose `depends_on` are all COMPLETE includes:
1. **story-100-next-app-scaffold** — unblocks all of 101–115 (entire app/landing/dashboard). HIGHEST leverage.
2. **story-56-smart-account-tests** — closes Epic E4 quality gate.
3. **story-61-vercel-ai-sdk-chat-api** — feeds story-108 TickCard streaming.
4. **story-131-mcp-server-setup** — extends story-130 toward 132/133/134.

Recommend **story-100** next: scaffolding the Next.js app unblocks the largest pending arc (16 stories) and is the only path to Best UI/UX + 20-Project Deployment Award eligibility.

---

## 10. Honest verdict

**Plan integrity: GOOD.** The 19 ADRs + 16 epics + 122 stories framework holds; 2026-06-09 audit corrections were absorbed; load-bearing gotchas are documented and tracked.

**Foundation integrity: STRONG.** 69 stories merged with full review discipline. Contracts, runtime phases, attestation pipeline, MCP stdio core, skill structure, wallet bootstrap, deploy runbook — all built to quality bar.

**Surface integrity: WEAK.** 3 of 4 user-facing surfaces are not buildable to demo today. The web app — the surface the Mantle prize doc and Best UI/UX track depend on — is a one-line `index.ts` stub.

**Risk:** if we keep shipping infrastructure stories and never start the web app, the submission risks scoring strong on architecture but failing eligibility on the "runnable frontend interface" requirement. The next story should pivot to **story-100**.

**Spec ↔ code coherence:** ~85%. Minor naming drift (runtime/agent, skill duplication, ui empty) is fixable in one cleanup PR before publish.

---

*Audit performed by Claude Code session 2026-06-14, against research/concierge/ (12 files read) + docs/PRD.md + docs/sprint-status.yaml + filesystem inventory of packages/ + apps/ + contracts/. No on-chain calls in this audit — verify story-193 readiness via `scripts/preflight-mainnet.sh` separately.*
